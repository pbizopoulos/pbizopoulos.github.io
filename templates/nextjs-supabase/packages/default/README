# Run Locally

# Copy .env.local from http://localhost:54323/project/default/settings/log-drains?showConnect=true&connectTab=frameworks to .env

# Start supabase
supabase start

# Reset supabase
supabase db reset

# Test supabase
supabase test db

# Lint supabase
supabase db lint --fail-on warning

# Install dependencies
npm install

# Run the app
npm run dev

# All tests
npm test
npm run test:coverage

# Update visual baselines
npm test --update-snapshots

# Open last HTML report run
npx playwright show-report

# Update all dependencies
npx npm-check-updates -u
